function createStart(params) {
    //creates a start
}

function basicPath(params) {
    //basic 1 way path
}

function splitPath(params) {
    //adv 2 way path
}

function removeElement(params) {
    //usable remove button
}

function reset(params) {
    //resets the whole tree
}

function savePlan(params) {
    //saves whole plan in either txt or picture
}

